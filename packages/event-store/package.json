{
  "name": "@animal-helper/event-store",
  "version": "0.0.0",
  "private": true,
  "description": "PostgreSQL event-store adapter for Animal Helper.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run",
    "test:integration": "vitest run test/apply-command.integration.test.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@animal-helper/contracts": "workspace:*",
    "@animal-helper/domain": "workspace:*",
    "postgres": "3.4.8"
  }
}
