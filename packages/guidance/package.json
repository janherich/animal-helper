{
  "name": "@animal-helper/guidance",
  "version": "0.0.0",
  "private": true,
  "description": "Code-owned catalog, instruction slots, and resolved injured-guidance copy.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "sync-catalog": "node scripts/build-catalog.mjs",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@animal-helper/contracts": "workspace:*"
  }
}
