{
  "name": "@animal-helper/client",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-free reporter command client for Animal Helper.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@animal-helper/contracts": "workspace:*",
    "@animal-helper/domain": "workspace:*"
  }
}
