# Edge Function entry point

The actual Supabase Edge Function will be added with the first versioned API
contract. It will be a thin HTTP adapter around application handlers and the
pure domain package.

Until then, this placeholder prevents a speculative endpoint or insecure
client-to-table API from becoming the accidental architecture.
